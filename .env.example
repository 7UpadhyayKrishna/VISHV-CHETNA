# Copy to .env (local) and to Vercel → Settings → Environment Variables

ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=change-this-session-secret

# MongoDB Atlas example (must also allow 0.0.0.0/0 in Network Access for Vercel)
MONGO_URL=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
DB_NAME=vishv_chetna
